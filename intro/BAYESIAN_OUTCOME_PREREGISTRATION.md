# Methodology document moved

The canonical, editable methodology copy is now maintained at [`site/content/methodology.md`](../site/content/methodology.md).

This pointer prevents two Markdown copies from drifting apart. The site generator reads the canonical file directly.
